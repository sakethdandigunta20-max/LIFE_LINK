# Base URL for the backend API. Leave as /api during local dev (Vite proxy handles it).
# For production (e.g. Vercel), set this to your deployed backend URL, e.g. https://api.yourdomain.com/api
VITE_API_URL=/api
